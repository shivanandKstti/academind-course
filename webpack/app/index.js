const cities = [
    "banglore",
    "gokak",
    "belagavi"
]

console.log(cities)
export default cities;

function greeting(name) {
    console.log(`hello ${name}`);
}

greeting('name');

const person = {
    name: 'name',
    address: 'address',
    phone: 111,
    city: 'City',
    email: 'email'
}

console.log(person);
console.log(person.name)